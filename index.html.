<!DOCTYPE html>
<html lang="hi">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Review Funnel — Demo</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600;700&family=Poppins:wght@400;500;600&family=JetBrains+Mono:wght@500&display=swap" rel="stylesheet">
<style>
  :root{
    --black:#08080a;
    --panel:#131315;
    --panel-2:#1a1a1d;
    --gold:#d4af37;
    --gold-light:#f1cf7c;
    --cream:#f4efe2;
    --muted:#8f897a;
    --border:rgba(212,175,55,0.22);
  }
  *{box-sizing:border-box;}
  body{
    margin:0;
    min-height:100vh;
    background:
      radial-gradient(circle at 50% -10%, rgba(212,175,55,0.10), transparent 55%),
      var(--black);
    font-family:'Poppins',sans-serif;
    color:var(--cream);
    display:flex;
    justify-content:center;
    padding:28px 16px 60px;
  }
  .app{ width:100%; max-width:430px; }

  .brandbar{
    display:flex; align-items:center; justify-content:center; gap:8px;
    margin-bottom:22px; opacity:0.85;
  }
  .brandbar .line{ width:20px; height:1px; background:var(--gold); }
  .brandbar span{
    font-family:'JetBrains Mono',monospace; font-size:11px; letter-spacing:2px;
    text-transform:uppercase; color:var(--gold-light);
  }

  /* Setup panel */
  details.setup{
    background:var(--panel); border:1px solid var(--border); border-radius:14px;
    margin-bottom:18px; padding:2px 16px;
  }
  details.setup summary{
    cursor:pointer; padding:12px 0; font-size:13px; color:var(--muted);
    list-style:none; display:flex; justify-content:space-between; align-items:center;
  }
  details.setup summary::-webkit-details-marker{ display:none; }
  details.setup summary .cap{ font-family:'JetBrains Mono',monospace; letter-spacing:1px; }
  .field{ margin-bottom:14px; }
  .field label{ display:block; font-size:12px; color:var(--muted); margin-bottom:6px; }
  .field input[type=text], .field input[type=url]{
    width:100%; background:var(--panel-2); border:1px solid var(--border);
    color:var(--cream); padding:10px 12px; border-radius:8px; font-size:13px;
    font-family:'Poppins',sans-serif;
  }
  .toggle-row{
    display:flex; align-items:flex-start; gap:10px; padding:10px 0 16px;
    border-top:1px solid var(--border); margin-top:4px;
  }
  .toggle-row input{ margin-top:3px; accent-color:var(--gold); }
  .toggle-row p{ margin:0; font-size:11.5px; color:var(--muted); line-height:1.5; }

  /* Card */
  .card{
    background:linear-gradient(180deg,var(--panel),var(--panel-2));
    border:1px solid var(--border); border-radius:20px;
    padding:32px 24px 26px; text-align:center;
    box-shadow:0 20px 50px rgba(0,0,0,0.45);
  }
  .step-tag{
    font-family:'JetBrains Mono',monospace; font-size:10.5px; letter-spacing:2.5px;
    color:var(--gold); text-transform:uppercase; margin-bottom:10px;
  }
  h1{
    font-family:'Playfair Display',serif; font-size:23px; font-weight:700;
    margin:0 0 8px; color:var(--cream);
  }
  .sub{ font-size:13px; color:var(--muted); margin:0 0 26px; line-height:1.6; }

  /* Stars */
  .stars{ display:flex; justify-content:center; gap:8px; margin-bottom:6px; }
  .star-btn{
    background:none; border:none; cursor:pointer; padding:4px;
    transform:scale(1); transition:transform .15s ease;
  }
  .star-btn:active{ transform:scale(0.88); }
  .star-btn svg{ width:34px; height:34px; }
  .star-label{ font-size:11.5px; color:var(--muted); margin-top:14px; min-height:16px; }

  /* seal */
  .seal{
    width:64px; height:64px; margin:6px auto 14px; border-radius:50%;
    border:2px solid var(--gold); display:flex; align-items:center; justify-content:center;
    color:var(--gold); font-size:26px; font-family:'Playfair Display',serif;
    animation:stampIn .4s cubic-bezier(.2,1.4,.4,1);
    box-shadow:0 0 0 4px rgba(212,175,55,0.08);
  }
  @keyframes stampIn{ from{ transform:scale(2) rotate(-8deg); opacity:0;} to{ transform:scale(1) rotate(0); opacity:1;} }

  .review-card{
    text-align:left; background:var(--panel-2); border:1px solid var(--border);
    border-radius:12px; padding:14px 16px; margin-bottom:12px;
  }
  .review-card p{ font-size:13.5px; line-height:1.6; margin:0 0 12px; color:var(--cream); }
  .review-actions{ display:flex; gap:8px; }
  .btn{
    flex:1; border:none; border-radius:8px; padding:10px 12px; font-size:12.5px;
    font-weight:600; cursor:pointer; font-family:'Poppins',sans-serif;
  }
  .btn-copy{ background:var(--panel); color:var(--gold-light); border:1px solid var(--border); }
  .btn-copy.copied{ color:#8fd19e; border-color:#8fd19e; }
  .btn-gold{
    background:linear-gradient(180deg,var(--gold-light),var(--gold)); color:#211a06;
  }
  .btn-gold:hover{ filter:brightness(1.05); }
  .btn-full{ width:100%; margin-top:4px; }
  .btn-ghost{
    background:none; border:1px solid var(--border); color:var(--muted);
    width:100%; padding:10px; border-radius:8px; font-size:12.5px; cursor:pointer;
    margin-top:14px; font-family:'Poppins',sans-serif;
  }

  textarea, .field2 input{
    width:100%; background:var(--panel-2); border:1px solid var(--border);
    color:var(--cream); padding:11px 13px; border-radius:10px; font-size:13.5px;
    font-family:'Poppins',sans-serif; margin-bottom:12px; resize:none;
  }
  textarea{ min-height:90px; line-height:1.5; }
  textarea::placeholder, .field2 input::placeholder{ color:#5c584d; }

  .thanks-icon{ font-size:30px; margin-bottom:8px; }
  .footnote{
    text-align:center; font-size:10.5px; color:#54504a; margin-top:20px; line-height:1.6;
  }
</style>
</head>
<body>
<div class="app">

  <div class="brandbar"><div class="line"></div><span>Review Funnel · Demo</span><div class="line"></div></div>

  <details class="setup" open>
    <summary><span class="cap">⚙ Setup (customer ko nahi dikhega)</span><span>▾</span></summary>
    <div class="field">
      <label>Dukaan / Business ka naam</label>
      <input type="text" id="bizName" value="Techmoin" placeholder="Jaise: Sharma Sweets">
    </div>
    <div class="field">
      <label>Google Review link (jahan 4–5★ wale bheje jaayenge)</label>
      <input type="url" id="googleLink" value="https://g.page/r/your-business/review" placeholder="https://g.page/r/xxxx/review">
    </div>
    <div class="toggle-row">
      <input type="checkbox" id="safeMode">
      <p><b>Policy-safe mode:</b> is par tick karoge to <b>1★ customer ko bhi</b> Google review ka button dikhega (chhupaya nahi jaayega) — sirf unhe pehle ek chhota private note bhi dikhega. Google ki policy "review gating" (bure review rokna) ko allow nahi karti, isliye ye mode safer hai.</p>
    </div>
  </details>

  <div class="card" id="cardArea"></div>

  <p class="footnote">Ye ek functional demo hai — apne QR code ko is page ke hosted link se point karna, tabhi customer scan karke ye khol payega.</p>
</div>

<script>
const bizNameEl = document.getElementById('bizName');
const googleLinkEl = document.getElementById('googleLink');
const safeModeEl = document.getElementById('safeMode');
const cardArea = document.getElementById('cardArea');

const positiveTemplates = {
  5: [
    "{biz} mein experience bahut hi zabardast raha! Staff friendly hai aur service time pe milti hai. Highly recommend karta hoon.",
    "Quality aur service dono top-notch! {biz} se bahut khush hoon, dobara zaroor aaunga.",
    "Best decision tha {biz} choose karna — professional team, achhi pricing, aur satisfying result mila."
  ],
  4: [
    "{biz} ka experience achha raha, service aur staff dono satisfying the. Recommend karunga.",
    "Overall achhi service mili {biz} se, thoda aur improve ho sakta hai lekin experience positive raha.",
    "Achha kaam, time pe delivery, aur reasonable price — {biz} ke saath experience accha raha."
  ]
};

let currentRating = 0;

function renderStars(){
  cardArea.innerHTML = `
    <div class="step-tag">Step 01</div>
    <h1>Aapka experience kaisa raha?</h1>
    <p class="sub">${bizNameEl.value || 'Hamare'} yahan aane ke liye shukriya — bas ek tap mein batayein.</p>
    <div class="stars" id="starsRow"></div>
    <div class="star-label" id="starLabel">&nbsp;</div>
  `;
  const row = document.getElementById('starsRow');
  const label = document.getElementById('starLabel');
  const labels = {1:'Bahut kharaab',2:'Theek nahi',3:'Average',4:'Achha',5:'Zabardast!'};
  for(let i=1;i<=5;i++){
    const b = document.createElement('button');
    b.className='star-btn';
    b.innerHTML = starSVG(false);
    b.onmouseenter = ()=> paintStars(i);
    b.onmouseleave = ()=> paintStars(0);
    b.onclick = ()=> { currentRating = i; handleRating(i); };
    row.appendChild(b);
  }
  function paintStars(n){
    [...row.children].forEach((btn,idx)=>{
      btn.innerHTML = starSVG(idx < n);
    });
    label.textContent = n ? labels[n] : '\u00A0';
  }
}

function starSVG(filled){
  return `<svg viewBox="0 0 24 24" fill="${filled ? 'url(#g)' : 'none'}" stroke="#d4af37" stroke-width="1.5">
    <defs><linearGradient id="g" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stop-color="#f1cf7c"/><stop offset="100%" stop-color="#d4af37"/>
    </linearGradient></defs>
    <path d="M12 2.5l2.9 6.4 6.9.7-5.2 4.8 1.5 6.9L12 17.9l-6.1 3.4 1.5-6.9L2.2 9.6l6.9-.7L12 2.5z"/>
  </svg>`;
}

function handleRating(n){
  const biz = bizNameEl.value || 'Hamare business';
  const link = googleLinkEl.value || '#';

  if(n >= 4){
    const opts = positiveTemplates[n].map(t => t.replace(/{biz}/g, biz));
    cardArea.innerHTML = `
      <div class="seal">✦</div>
      <div class="step-tag">Step 02</div>
      <h1>Shukriya! 🎉</h1>
      <p class="sub">Neeche diye gaye kisi bhi review ko copy karke Google par paste kar dijiye — 10 second ka kaam hai.</p>
      <div id="reviewOpts"></div>
      <button class="btn-ghost" onclick="renderStars()">← Wapas</button>
    `;
    const wrap = document.getElementById('reviewOpts');
    opts.forEach((text,i)=>{
      const el = document.createElement('div');
      el.className='review-card';
      el.innerHTML = `
        <p>${text}</p>
        <div class="review-actions">
          <button class="btn btn-copy" data-text="${encodeURIComponent(text)}">Copy karein</button>
          <button class="btn btn-gold" data-link="${link}">Google par jaayein →</button>
        </div>`;
      wrap.appendChild(el);
    });
    wrap.querySelectorAll('.btn-copy').forEach(btn=>{
      btn.onclick = ()=>{
        const text = decodeURIComponent(btn.dataset.text);
        navigator.clipboard?.writeText(text);
        btn.textContent = 'Copy ho gaya ✓';
        btn.classList.add('copied');
        setTimeout(()=>{ btn.textContent='Copy karein'; btn.classList.remove('copied'); }, 2000);
      };
    });
    wrap.querySelectorAll('.btn-gold').forEach(btn=>{
      btn.onclick = ()=> window.open(btn.dataset.link, '_blank');
    });
  } else {
    const showGoogleToo = safeModeEl.checked;
    cardArea.innerHTML = `
      <div class="step-tag">Step 02</div>
      <h1>Batayein kya kami rahi</h1>
      <p class="sub">Aapki baat seedhi ${biz} tak pahunchegi, taaki hum isse theek kar sakein.</p>
      <textarea id="fbText" placeholder="Yahan apni feedback likhein..."></textarea>
      <div class="field2">
        <input type="text" id="fbPhone" placeholder="Phone number (optional)">
      </div>
      <button class="btn btn-gold btn-full" onclick="submitFeedback(${showGoogleToo})">Feedback bhejein</button>
      ${showGoogleToo ? `<button class="btn-ghost" onclick="window.open('${link}','_blank')">Fir bhi Google par review likhna hai →</button>` : ''}
      <button class="btn-ghost" onclick="renderStars()">← Wapas</button>
    `;
  }
}

function submitFeedback(showGoogleToo){
  const biz = bizNameEl.value || 'Hume';
  cardArea.innerHTML = `
    <div class="thanks-icon">🙏</div>
    <div class="step-tag">Step 03</div>
    <h1>Dhanyawad!</h1>
    <p class="sub">Aapki feedback ${biz} tak pahunch gayi hai. Hum ise jald improve karne ki koshish karenge.</p>
    <button class="btn-ghost" onclick="renderStars()">Naya customer / Reset</button>
  `;
}

bizNameEl.addEventListener('input', ()=>{ if(currentRating===0) renderStars(); });
renderStars();
</script>
</body>
</html>
